# Public, local-only demo credentials; never activate this profile on a public server.
spring.datasource.password=${DB_PASSWORD:demo-database-password}
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:demo/data.sql
app.admin.email=${ADMIN_EMAIL:admin@example.test}
app.admin.password=${ADMIN_PASSWORD:demo-admin-password}
app.demo.enabled=true
